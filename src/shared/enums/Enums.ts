export enum LocationRefreshStatus {
    Refreshing = 1,
    Success = 2,
    Error = 3,
    NotFound = 4
}

export enum WeatherStatus {
    Refreshing = 1,
    Success = 2,
    Error = 3,
    NotFound = 4
}